var position = new google.maps.LatLng(<%= @google_place.lat %>,
                                        <%= @google_place.lng %>);

<% if current_user.beers.include?(@beer) %>
  addMarker(position, map);
<% end %>
